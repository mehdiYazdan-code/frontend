import React, {forwardRef} from 'react';
import DatePicker from "react-multi-date-picker";

const ShamsiDatePicker = forwardRef((props, ref) => {
    return (
        <DatePicker/>
    )
})

export default ShamsiDatePicker;